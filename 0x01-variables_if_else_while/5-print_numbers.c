#include <stdio.h>
/**
 * main - a program that writes numers
 * Return: Always 0(success)
 */
/* Betty always there for a checkin */

int main(void)
{
	int n = '0';

	for (n = '0'; n <= '10'; n++)
	{
		putchar(n);
	} putchar('\n')

