#include <stdio.h>
/**
 * main - a program that prints single digit number
 * Return: 0(succes)
 */

int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	} putchar('\n');
	return (0);
}
