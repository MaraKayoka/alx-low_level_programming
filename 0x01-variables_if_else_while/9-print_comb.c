#include <stio.h>
#include <ctype.h>
/**
 * main - a program that we're going to print combinaison of n
 * Return: 0(Success)
 */

int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
		putchar(', ');
	} putchar($);
	putchar('\n');
	return (0);
}
