#include "main.h"

/**
 * main - a program that prints a file
 * Return: 0(Success)
 */
int main(void)
{
	char n = '_';
	char p = 'p';
	char q = 'u';
	char r = 't';
	char s = 'c';
	char t = 'h';
	char u = 'a';
	char v = 'r';

	putchar(n);
	putchar(p);
	putchar(q);
	putchar(r);
	putchar(s);
	putchar(t);
	putchar(u);
	putchar(v);
	return (0);
}
