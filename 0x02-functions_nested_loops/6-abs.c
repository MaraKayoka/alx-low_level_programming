#include "main.h"
/**
 * _abs - a program that computes the absolute value of a number
 * @p: The input integer.
 * Return: 0(Always)
 */
int _abs(int p)
{
	if (p >= 0)
	{
		return (p);
	} return (-p);

}
